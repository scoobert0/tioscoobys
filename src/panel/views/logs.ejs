<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        .animate-float { animation: float 6s ease-in-out infinite; }
        
        .glass {
            background: rgba(30, 41, 59, 0.4);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(148, 163, 184, 0.2);
        }
    </style>
</head>

<body class="bg-gradient-to-br from-slate-900 via-blue-900/30 to-slate-900 text-gray-100 min-h-screen">

    <!-- HEADER -->
    <header class="backdrop-blur-lg bg-slate-900/70 border-b border-blue-500/20 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-2xl flex items-center justify-center text-3xl shadow-2xl shadow-blue-500/30 animate-float">
                    📊
                </div>
                <h1 class="text-3xl font-bold bg-gradient-to-r from-blue-400 to-cyan-300 bg-clip-text text-transparent">
                    <%= title %>
                </h1>
            </div>

            <div class="flex items-center gap-8">
                <span class="text-sm text-gray-300">
                    Olá, <span class="font-semibold text-white"><%= username %></span> 👋
                </span>
                <div class="flex gap-4">
                    <a href="/dashboard" class="px-6 py-3 text-sm font-medium bg-blue-600/20 text-blue-300 rounded-xl hover:bg-blue-600/40 hover:scale-105 transition-all shadow-md">
                        Dashboard
                    </a>
                    <a href="/logout" class="px-6 py-3 text-sm font-medium bg-red-600/20 text-red-400 rounded-xl hover:bg-red-600/30 hover:scale-105 transition-all shadow-md">
                        Sair
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- MAIN -->
    <main class="max-w-7xl mx-auto px-6 py-10">

        <div class="mb-12">
            <h2 class="text-4xl font-bold text-white mb-3">Seus Logs de Atividade</h2>
            <p class="text-xl text-gray-400">Acompanhe todas as ações realizadas na sua conta em tempo real</p>
        </div>

        <!-- LOGS TABLE -->
        <div class="glass rounded-3xl p-10 shadow-2xl shadow-blue-500/20 border-blue-500/30">
            <% if (activityLogs.length === 0) { %>
                <div class="text-center py-20">
                    <div class="text-8xl mb-6 text-gray-600">📭</div>
                    <p class="text-2xl text-gray-400">Nenhum log de atividade encontrado.</p>
                    <p class="text-lg text-gray-500 mt-4">Comece a usar a plataforma para ver o histórico aqui.</p>
                </div>
            <% } else { %>
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-blue-500/30">
                        <thead>
                            <tr class="bg-slate-800/50">
                                <th class="px-8 py-6 text-left text-sm font-bold text-cyan-300 uppercase tracking-wider">Data e Hora</th>
                                <th class="px-8 py-6 text-left text-sm font-bold text-cyan-300 uppercase tracking-wider">Evento</th>
                                <th class="px-8 py-6 text-left text-sm font-bold text-cyan-300 uppercase tracking-wider">Detalhes</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-blue-500/20">
                            <% activityLogs.forEach(function(log) { %>
                                <tr class="hover:bg-slate-800/40 transition-all duration-300">
                                    <td class="px-8 py-6 text-sm font-medium text-gray-200">
                                        <%= new Date(log.timestamp).toLocaleString('pt-BR', { 
                                            day: '2-digit', 
                                            month: '2-digit', 
                                            year: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit',
                                            second: '2-digit'
                                        }) %>
                                    </td>
                                    <td class="px-8 py-6">
                                        <span class="px-4 py-2 text-sm font-semibold rounded-full bg-blue-600/30 text-blue-300 border border-blue-500/50">
                                            <%= log.event_type %>
                                        </span>
                                    </td>
                                    <td class="px-8 py-6 text-sm text-gray-300 break-words max-w-lg">
                                        <%= log.details %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>

                <!-- Optional: Pagination or load more (if you have many logs) -->
                <div class="mt-10 text-center">
                    <p class="text-sm text-gray-500">
                        Mostrando os <%= activityLogs.length %> logs mais recentes
                    </p>
                </div>
            <% } %>
        </div>

    </main>
</body>
</html>