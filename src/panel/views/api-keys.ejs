<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        .animate-float { animation: float 6s ease-in-out infinite; }
        
        .glass {
            background: rgba(30, 41, 59, 0.4);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(148, 163, 184, 0.2);
        }
    </style>

    <script>
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                alert('API Key copiada para a área de transferência! 🔑');
            }, (err) => {
                console.error('Erro ao copiar:', err);
                alert('Falha ao copiar a API Key.');
            });
        }
    </script>
</head>

<body class="bg-gradient-to-br from-slate-900 via-blue-900/30 to-slate-900 text-gray-100 min-h-screen">

    <!-- HEADER -->
    <header class="backdrop-blur-lg bg-slate-900/70 border-b border-blue-500/20 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 py-5 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-2xl flex items-center justify-center text-3xl shadow-2xl shadow-blue-500/30 animate-float">
                    🔑
                </div>
                <h1 class="text-3xl font-bold bg-gradient-to-r from-blue-400 to-cyan-300 bg-clip-text text-transparent">
                    <%= title %>
                </h1>
            </div>

            <div class="flex items-center gap-8">
                <span class="text-sm text-gray-300">
                    Olá, <span class="font-semibold text-white"><%= username %></span> 👋
                </span>
                <div class="flex gap-4">
                    <a href="/dashboard" class="px-6 py-3 text-sm font-medium bg-blue-600/20 text-blue-300 rounded-xl hover:bg-blue-600/40 hover:scale-105 transition-all shadow-md">
                        Dashboard
                    </a>
                    <a href="/logout" class="px-6 py-3 text-sm font-medium bg-red-600/20 text-red-400 rounded-xl hover:bg-red-600/30 hover:scale-105 transition-all shadow-md">
                        Sair
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- MAIN -->
    <main class="max-w-7xl mx-auto px-6 py-10">

        <div class="mb-12">
            <h2 class="text-4xl font-bold text-white mb-3">Suas API Keys</h2>
            <p class="text-xl text-gray-400">Gerencie suas chaves de acesso à API com segurança</p>
        </div>

        <!-- MESSAGES -->
        <% if (message) { %>
            <div class="glass border border-green-500/50 rounded-2xl p-6 mb-10 shadow-xl shadow-green-500/20">
                <p class="text-green-300 text-lg font-medium"><%= message %></p>
            </div>
        <% } %>
        <% if (error) { %>
            <div class="glass border border-red-500/50 rounded-2xl p-6 mb-10 shadow-xl shadow-red-500/20">
                <p class="text-red-300 text-lg font-medium"><%= error %></p>
            </div>
        <% } %>

        <!-- CREATE NEW KEY -->
        <div class="mb-12">
            <form action="/api-keys" method="POST">
                <button type="submit" 
                        class="inline-flex items-center gap-3 px-8 py-5 text-xl font-bold text-white bg-gradient-to-r from-blue-600 to-cyan-600 rounded-2xl hover:from-blue-700 hover:to-cyan-700 transform hover:scale-105 transition-all shadow-xl shadow-blue-500/50">
                    <span class="text-2xl">✨</span>
                    Criar Nova API Key
                </button>
            </form>
        </div>

        <!-- API KEYS LIST -->
        <% if (apiKeys.length === 0) { %>
            <div class="glass rounded-3xl p-20 text-center shadow-2xl shadow-blue-500/20 border-blue-500/30">
                <div class="text-8xl mb-8 text-gray-600">🔒</div>
                <p class="text-2xl text-gray-400 mb-4">Você ainda não tem nenhuma API Key</p>
                <p class="text-lg text-gray-500">Crie sua primeira chave acima para começar a integrar com a API!</p>
            </div>
        <% } else { %>
            <div class="space-y-8">
                <% apiKeys.forEach(function(key) { %>
                    <div class="glass rounded-3xl p-8 shadow-xl shadow-blue-500/20 border-blue-500/30 hover:shadow-2xl hover:shadow-cyan-500/30 hover:scale-[1.01] transition-all duration-300">
                        <div class="flex flex-col lg:flex-row lg:items-center justify-between gap-8">
                            <div class="flex-1">
                                <div class="mb-6">
                                    <p class="text-sm text-gray-400 mb-2">Chave de API</p>
                                    <div class="flex items-center gap-4">
                                        <code class="font-mono text-cyan-300 text-lg break-all bg-slate-800/60 px-5 py-3 rounded-xl border border-cyan-500/40">
                                            <%= key.api_key %>
                                        </code>
                                        <button onclick="copyToClipboard('<%= key.api_key %>')" 
                                                class="px-6 py-3 bg-gradient-to-r from-blue-600 to-cyan-600 text-white font-bold rounded-xl hover:from-blue-700 hover:to-cyan-700 transform hover:scale-110 transition-all shadow-lg">
                                            📋 Copiar
                                        </button>
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-sm">
                                    <div>
                                        <span class="text-gray-400">Plano:</span>
                                        <span class="ml-2 text-white font-semibold"><%= key.plan %></span>
                                    </div>
                                    <div>
                                        <span class="text-gray-400">Expira em:</span>
                                        <span class="ml-2 text-white font-semibold"><%= new Date(key.expires_at).toLocaleDateString('pt-BR') %></span>
                                    </div>
                                    <div>
                                        <span class="text-gray-400">Uso mensal:</span>
                                        <span class="ml-2 text-white font-semibold"><%= key.requests_month %> requests</span>
                                    </div>
                                </div>

                                <div class="mt-4">
                                    <span class="text-gray-400">Último uso:</span>
                                    <span class="ml-2 text-white font-medium">
                                        <%= key.last_used_at ? new Date(key.last_used_at).toLocaleString('pt-BR') : 'Nunca usado' %>
                                    </span>
                                </div>
                            </div>

                            <div class="flex gap-4">
                                <form action="/api-keys/<%= key.id %>/regenerate" method="POST">
                                    <button type="submit" 
                                            class="px-8 py-4 bg-gradient-to-r from-yellow-600 to-amber-600 text-white font-bold rounded-xl hover:from-yellow-700 hover:to-amber-700 transform hover:scale-105 transition-all shadow-lg shadow-yellow-500/40">
                                        🔄 Regenerar
                                    </button>
                                </form>

                                <form action="/api-keys/<%= key.id %>/delete" method="POST" 
                                      onsubmit="return confirm('Tem certeza que deseja remover esta API Key? Esta ação é irreversível!')">
                                    <button type="submit" 
                                            class="px-8 py-4 bg-gradient-to-r from-red-600 to-rose-600 text-white font-bold rounded-xl hover:from-red-700 hover:to-rose-700 transform hover:scale-105 transition-all shadow-lg shadow-red-500/40">
                                        🗑️ Remover
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>

    </main>
</body>
</html>