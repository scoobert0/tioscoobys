<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8" />
    <title><%= title || 'Dashboard' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }
        .animate-float { animation: float 6s ease-in-out infinite; }

        .glass {
            background: rgba(30, 41, 59, 0.45);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(148, 163, 184, 0.25);
        }

        /* Melhora performance em mobile */
        @media (hover: none) {
            .group:hover\\:scale-110 { transform: none !important; }
            .hover\\:scale-105 { transform: none !important; }
        }
    </style>
</head>

<body class="bg-gradient-to-br from-slate-900 via-purple-900/20 to-slate-900 text-gray-100 min-h-screen">

    <!-- HEADER -->
    <header class="backdrop-blur-xl bg-slate-900/90 border-b border-blue-500/30 sticky top-0 z-50 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
            <!-- Logo + Título -->
            <div class="flex items-center gap-3">
                <div class="w-11 h-11 bg-gradient-to-br from-blue-500 to-cyan-500 rounded-2xl flex items-center justify-center text-3xl shadow-xl shadow-blue-600/40 animate-float">
                    🚀
                </div>
                <h1 class="text-2xl font-extrabold bg-gradient-to-r from-blue-400 to-cyan-300 bg-clip-text text-transparent">
                    Dashboard
                </h1>
            </div>

            <!-- Menu Desktop -->
            <nav class="hidden md:flex items-center gap-8">
                <div class="flex items-center gap-4 text-sm">
                    <span class="text-gray-300">Olá,</span>
                    <span class="font-bold text-white"><%= username %></span>
                    <span>👋</span>
                </div>
                <div class="flex gap-3">
                    <a href="/profile" class="px-6 py-2.5 text-sm font-medium text-blue-300 hover:text-cyan-300 bg-white/5 rounded-xl transition">
                        Perfil
                    </a>
                    <a href="/logout" class="px-6 py-2.5 text-sm font-medium bg-red-600/30 text-red-400 rounded-xl hover:bg-red-600/40 transition">
                        Sair
                    </a>
                </div>
            </nav>

            <!-- Botão Menu Mobile -->
            <button id="mobile-menu-btn" class="md:hidden text-3xl text-gray-200 focus:outline-none" aria-label="Menu">
                ☰
            </button>
        </div>
    </header>

    <!-- Overlay + Menu Mobile -->
    <div id="mobile-overlay" class="fixed inset-0 bg-black/70 z-40 hidden"></div>
    <div id="mobile-menu" class="fixed right-0 top-0 h-full w-80 max-w-full bg-slate-900/95 backdrop-blur-2xl border-l border-blue-500/30 z-50 transform translate-x-full transition-transform duration-300">
        <div class="p-6">
            <div class="flex items-center justify-between mb-10">
                <div class="flex items-center gap-3">
                    <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-cyan-500 rounded-2xl flex items-center justify-center text-3xl shadow-xl">
                        🚀
                    </div>
                    <span class="text-xl font-bold text-white"><%= username %></span>
                </div>
                <button id="mobile-close-btn" class="text-3xl text-gray-400 hover:text-white">✕</button>
            </div>
            <nav class="flex flex-col gap-4">
                <a href="/profile" class="px-6 py-4 text-lg text-blue-300 bg-white/5 rounded-2xl hover:bg-white/10 transition text-center">
                    👤 Perfil
                </a>
                <a href="/billing" class="px-6 py-4 text-lg text-cyan-300 bg-white/5 rounded-2xl hover:bg-white/10 transition text-center">
                    💳 Billing
                </a>
                <a href="/logs" class="px-6 py-4 text-lg text-gray-200 bg-white/5 rounded-2xl hover:bg-white/10 transition text-center">
                    📊 Logs
                </a>
                <a href="/docs" class="px-6 py-4 text-lg text-gray-200 bg-white/5 rounded-2xl hover:bg-white/10 transition text-center">
                    📘 Documentação
                </a>
                <a href="/logout" class="px-6 py-4 text-lg font-medium bg-red-600/30 text-red-400 rounded-2xl hover:bg-red-600/40 transition text-center">
                    🚪 Sair
                </a>
            </nav>
        </div>
    </div>

    <!-- MAIN -->
    <main class="max-w-7xl mx-auto px-4 py-8 pb-20">
        <!-- Visão Geral -->
        <section class="mb-10">
            <h2 class="text-3xl md:text-4xl font-bold text-white mb-3">Visão Geral</h2>
            <p class="text-lg text-gray-400">Tudo o que você precisa em um só lugar</p>
        </section>

        <!-- PLANO ATUAL -->
        <section class="glass rounded-3xl p-6 md:p-10 mb-10 shadow-2xl shadow-blue-600/30 border-blue-500/40 relative overflow-hidden">
            <div class="absolute inset-0 bg-gradient-to-br from-blue-600/10 to-cyan-600/10"></div>
            <div class="relative z-10">
                <h3 class="text-2xl md:text-3xl font-bold text-white mb-8 flex items-center gap-3">
                    <span class="text-4xl">💎</span> Seu Plano Atual
                </h3>

                <div class="grid grid-cols-1 gap-6 mb-10">
                    <div class="bg-slate-800/70 rounded-2xl p-6 border border-blue-500/40">
                        <p class="text-gray-400 text-sm mb-2">Plano</p>
                        <p class="text-3xl md:text-4xl font-extrabold text-cyan-400"><%= plan.name %></p>
                    </div>

                    <div class="bg-slate-800/70 rounded-2xl p-6 border border-blue-500/40">
                        <p class="text-gray-400 text-sm mb-3">Uso Mensal</p>
                        <p class="text-2xl font-bold text-white mb-4"><%= plan.used %> / <%= plan.limit %> requests</p>
                        <div class="w-full bg-slate-700 rounded-full h-5 overflow-hidden">
                            <div class="bg-gradient-to-r from-blue-500 to-cyan-500 h-5 rounded-full transition-all duration-1000"
                                 style="width: <%= (plan.used / plan.limit * 100) %>%"></div>
                        </div>
                        <p class="text-sm text-gray-400 mt-3"><%= Math.round(plan.used / plan.limit * 100) %>% utilizado</p>
                    </div>

                    <div class="bg-slate-800/70 rounded-2xl p-6 border border-green-500/40">
                        <p class="text-gray-400 text-sm mb-2">Renovação</p>
                        <p class="text-2xl md:text-3xl font-bold text-green-400"><%= plan.expiresAt %></p>
                    </div>
                </div>

                <a href="/billing" class="block w-full md:w-auto md:inline-flex justify-center items-center gap-3 px-8 py-4 bg-gradient-to-r from-blue-600 to-cyan-600 text-white font-bold text-lg rounded-2xl hover:from-blue-700 hover:to-cyan-700 active:scale-95 transition-all shadow-xl shadow-blue-600/50">
                    <span class="text-2xl">✨</span>
                    Gerenciar Plano
                </a>
            </div>
        </section>

        <!-- MÉTRICAS -->
        <section class="grid grid-cols-2 lg:grid-cols-4 gap-5 mb-12">
            <div class="glass rounded-3xl p-6 text-center border border-slate-600">
                <p class="text-gray-400 text-sm mb-2">Hoje</p>
                <p class="text-4xl md:text-5xl font-bold text-white"><%= stats.requestsToday %></p>
            </div>
            <div class="glass rounded-3xl p-6 text-center border border-slate-600">
                <p class="text-gray-400 text-sm mb-2">Este Mês</p>
                <p class="text-4xl md:text-5xl font-bold text-white"><%= stats.requestsMonth %></p>
            </div>
            <div class="glass rounded-3xl p-6 text-center border border-red-900/50">
                <p class="text-red-400 text-sm mb-2">Erros</p>
                <p class="text-4xl md:text-5xl font-bold text-red-400"><%= stats.errors %></p>
            </div>
            <div class="glass rounded-3xl p-6 text-center border border-green-900/50">
                <p class="text-green-400 text-sm mb-2">PIX Pagos</p>
                <p class="text-3xl md:text-5xl font-bold text-green-400">R$ <%= stats.pixPaid %></p>
            </div>
        </section>

        <!-- AÇÕES RÁPIDAS -->
        <section class="mb-12">
            <h2 class="text-2xl md:text-3xl font-bold text-white mb-6">Ações Rápidas</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-5">
                <a href="/api-keys" class="group glass rounded-3xl p-8 text-center transition active:scale-95">
                    <div class="text-6xl mb-4 group-hover:animate-float">🔑</div>
                    <p class="text-xl font-bold text-white">API Keys</p>
                    <p class="text-sm text-gray-400 mt-1"><%= apiKeysCount %> chaves</p>
                </a>
                <a href="/billing" class="group glass rounded-3xl p-8 text-center transition active:scale-95">
                    <div class="text-6xl mb-4 group-hover:animate-float">💳</div>
                    <p class="text-xl font-bold text-white">Billing</p>
                </a>
                <a href="/logs" class="group glass rounded-3xl p-8 text-center transition active:scale-95">
                    <div class="text-6xl mb-4 group-hover:animate-float">📊</div>
                    <p class="text-xl font-bold text-white">Logs</p>
                </a>
                <a href="/docs" class="group glass rounded-3xl p-8 text-center transition active:scale-95">
                    <div class="text-6xl mb-4 group-hover:animate-float">📘</div>
                    <p class="text-xl font-bold text-white">Docs</p>
                </a>
            </div>
        </section>

        <!-- CONSULTAS DISPONÍVEIS -->
        <section>
            <h2 class="text-2xl md:text-3xl font-bold text-white mb-6">Consultas Disponíveis</h2>
            <div class="grid grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-5">
                <% 
                const consultas = [
                    { href: "/search/cpf", emoji: "🔍", label: "CPF" },
                    { href: "/search/name-cpf", emoji: "👤", label: "Nome / CPF" },
                    { href: "/search/phone", emoji: "📞", label: "Telefone" },
                    { href: "/search/cep", emoji: "📫", label: "CEP" },
                    { href: "/search/plate", emoji: "🚗", label: "Placa" },
                    { href: "/search/chassi", emoji: "⚙️", label: "Chassi" },
                    { href: "/search/mother-name", emoji: "👩‍👧‍👦", label: "Nome da Mãe" },
                    { href: "/search/score-cpf", emoji: "💯", label: "Score CPF" },
                    { href: "/search/rg", emoji: "🆔", label: "RG" },
                    { href: "/search/photo-cpf", emoji: "📸", label: "Foto CPF" },
                    { href: "/search/siblings", emoji: "👨‍👩‍👧‍👦", label: "Irmãos" }
                ];
                %>
                <% consultas.forEach(item => { %>
                <a href="<%= item.href %>" class="group glass rounded-3xl p-6 text-center transition active:scale-95">
                    <div class="text-5xl md:text-6xl mb-3 group-hover:animate-float"><%= item.emoji %></div>
                    <p class="text-sm md:text-base font-semibold text-gray-200 group-hover:text-white"><%= item.label %></p>
                </a>
                <% }) %>
            </div>
        </section>
    </main>

    <!-- Script Menu Mobile -->
    <script>
        const btn = document.getElementById('mobile-menu-btn');
        const closeBtn = document.getElementById('mobile-close-btn');
        const menu = document.getElementById('mobile-menu');
        const overlay = document.getElementById('mobile-overlay');

        function openMenu() {
            menu.classList.remove('translate-x-full');
            overlay.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
        }

        function closeMenu() {
            menu.classList.add('translate-x-full');
            overlay.classList.add('hidden');
            document.body.style.overflow = '';
        }

        btn.addEventListener('click', openMenu);
        closeBtn.addEventListener('click', closeMenu);
        overlay.addEventListener('click', closeMenu);
    </script>
</body>
</html>